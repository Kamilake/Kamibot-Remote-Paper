name: Release

on:
  push:
    branches:
      - main
      - 'refs/tags/*'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        minecraft-version: [ '1.20', '1.21' ]

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up JDK
      uses: actions/setup-java@v2
      with:
        distribution: 'adopt'
        java-version: '21'

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build with Gradle
      run: ./gradlew build -PreleaseVersion=${{ github.sha }} -PminecraftVersion=${{ matrix.minecraft-version }}

  release:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.event.release.tag_name }}
        release_name: Release ${{ github.event.release.tag_name }}
        draft: false
        prerelease: false

    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./build/libs/app-all.jar
        asset_name: Kamibot-Remote-Paper_${{ matrix.minecraft-version }}_${{ github.sha }}.jar
        asset_content_type: application/zip